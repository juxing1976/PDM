# ========== 数据库配置 ==========  
# 必填：MySQL 8.0容器配置  
MYSQL_ROOT_PASSWORD=$(cat mysql_root_pass)  # 强密码建议：openssl rand -base64 16  
DB_HOST=jx-mysql                    # 容器服务名（勿改）  
DB_NAME=pdm_dev                     # 主业务数据库  
DB_USER=pdm_user  
DB_PASSWORD=  $(cat db_app_pass)         # 应用连接密码（需加密存储）  
 
# ========== 应用配置 ==========  
API_BASE_URL=http://api-service:3000  # 后端服务地址（容器内通信）  
TAG=latest                          # 镜像版本标签（默认latest，生产环境建议固定版本号）  
 
# ========== 安全增强配置 ==========  
# 可选：通过 Docker Secrets 或 Vault 管理以下字段  
# MYSQL_ROOT_PASSWORD_FILE=/run/secrets/mysql_root_pass  
# DB_PASSWORD_FILE=/run/secrets/db_app_pass  
